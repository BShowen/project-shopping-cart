import { render, screen } from "@testing-library/react";
import { Cart } from "../components/Cart";

describe("Cart display component", () => {
  it.todo("Is hidden by default");
  it.todo("Is displayed when isOpen is true");
});
